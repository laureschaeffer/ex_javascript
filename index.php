<?php

//------------------------------------notes

// -	Elément.addEventListener(arg1, arg2) : à chaque événement (par exemple click, keydown ou input) une fonction est appelée, en deuxième argument
// -	élément.cloneNode(bool) : renvoie le clone de l’élément : "If true, then the node and its whole subtree, including text that may be in child Text nodes, is also copied. If false, only the node will be cloned. The subtree, including any text that the node contains, is not cloned."
// -	Element.value : attribue une valeur à cet élément 
// -	Element1.appendChild(élément2) : en lien avec node, ajoute l’élément 2 à l’élément 1(le container)
// -	Endroit.innerHTML= changement : modifie la valeur de « endroit » (dans html) par « changement »
// -	quareStyle = window.getComputedStyle(square)
// sortir les infos css de square
// -	squareStyle.getPropertyValue("background-color")
// choisir l’info qu’on veut
// toFixed(n) arrondi
//valeur des touches du clavier
// https://developer.mozilla.org/fr/docs/Web/API/KeyboardEvent/key 
